<h1><%= @creator.name %></h1>


 <% if @creator.portrait.attached? %>
  <%= image_tag @creator.portrait %>
<% end %> 


<h2> Biography: </h2>
<p> <%= @creator.bio %> </p>

<h2> Musicals: </h2>
<ul>
    <% @musicals.each do |musical|%>
    <li> <%= link_to musical.title, musical_path(musical) %> </li>
    <button class="p-0.5 border-red-700 border-2 rounded">Remove <button>
    <%
=begin%>
 <%= link_to 'Remove', book_path(book), method: :delete %> 
<%
=end%>
    <% end %>
</ul>

<%
=begin%>
 <p> <%= link_to "Add Musical to Person", '/books/new' %> </p> 
<%
=end%>

<div class="p-1 border-green-300 border-2 mt-4 max-w-min">
<h4>Add Musical</h4>
<%= form_with(model: Book.new, local: true) do |form| %>

    <%= form.hidden_field :creator_id, value: @creator.id %>

    <div class="field">
        <%= form.label :musical %>
        <%= form.select :musical_id, options_from_collection_for_select(Musical.all.sort_by {|hash| hash[:title]}, 'id', 'title') %>
    </div> 

    <%= submit_tag("Add Musical") %>

<% end %>
</div>

<p> <%= link_to "Edit Person", edit_creator_path(@creator) %> </p>

<p> <%= link_to 'Delete', creator_path(@creator.id), method: :delete %> </p>

<p> <%= link_to 'Back to Creators', creators_path %> </p>