<h1 class="font-bold text-xl">Creators</h1>
<ul>
    <% @creators.each do |creator|%>
    <li> <%= link_to creator.name, creator_path(creator) %> </li>
    <% end %>
</ul>

<p> <%= link_to "New Creator", new_creator_path %> </p>