<!DOCTYPE html>
<html>
  <head>
    <title>KintoTheWoods</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="bg-black bg-center bg-cover bg-fixed text-white font-medium antialiased" style="background-image:url(<%= image_url('backgrounds/curtains2.jpeg') %>)">
  <header class="my-2 border-b-2">
  <h1 class="font-bold text-2xl mb-2 flex justify-center"> <span class="text-yellow-400">Kin.</span>to the Woods </h1>
  <nav class="flex justify-center pb-2">
    <%= link_to 'Home', '/' %>
    <% if current_user %>
      <p class="px-1 mx-8"> Hi <%= link_to current_user.name, user_path(current_user) %></p>
      <%= link_to 'Log out', logout_path, method: :delete %>
    <% else %>
      <p class="px-1 mx-8"><%= link_to 'Log in', login_path %></p>
      <%= link_to 'Sign up', new_user_path %>
    <% end %>
    </nav>
  </header>
    <main class="mx-4">

    <%= yield %> 
    </main>
  </body>
</html>
