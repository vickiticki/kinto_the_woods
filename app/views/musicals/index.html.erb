<h1 class="font-bold text-xl mb-2">Musicals</h1>
<ul>
    <% @musicals.each do |musical|%>
    <li> <%= link_to musical.title, musical_path(musical) %> </li>

    <% end %>
</ul>

<p class="mt-4 text-green-200"> <%= link_to "Add Musical", new_musical_path %> </p>